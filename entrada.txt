// Declaração de variáveis
int idade
float altura
string nome
int numeros[5]

// Atribuição de valores
idade = 25
altura = 1.75
nome = "João Silva"

// Atribuição em array
numeros[0] = 10
numeros[1] = 20
numeros[2] = 30

// Operações aritméticas
float imc = 70 / (altura * altura)

// Estrutura condicional
IF (idade > 18) {
    PRINTT("Maior de idade")
    PRINT(imc)
} ELSE {
    PRINTT("Menor de idade")
}

// Estrutura de repetição
WHILE (idade < 30) {
    PRINT(idade)
    idade = idade + 1
}

// Operações lógicas
IF (altura >= 1.70 && altura <= 1.90) {
    PRINTT("Altura normal")
}

// Saída de array
PRINT(numeros[0])
PRINT(numeros[1])